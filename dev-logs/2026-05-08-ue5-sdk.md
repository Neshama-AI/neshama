# UE5 C++ SDK 编译验证日志

## 日期
2026-05-08 13:07 ~ 14:33

## 目标
使 NeshamaSDK 在 UE 5.6.1 中编译通过

## 环境
- UE5: 5.6.1 (buildversion ++UE5+Release-5.6-CL-44394996)
- OS: Windows 11 (25H2) 10.0.26200.8328
- CPU: AMD Ryzen 9 5900HX
- VS: Visual Studio 2022 BuildTools, MSVC 14.50.35730（UE5提示非首选14.38.33130，不阻塞）
- 项目路径: `C:\Users\刘舟\Documents\Unreal Projects\我的项目\`
- 插件路径: `Plugins\NeshamaSDK\`

## 编译轮次

### R1 — Build.cs Definitions不存在
**时间**: ~13:33
**错误**: `NeshamaSDK.Build.cs`和`NeshamaSDKEditor.Build.cs`中使用了`Definitions`属性，UE5不存在
**修复**: `Definitions` → `PrivateDefinitions`
**Commit**: ec7b855
**状态**: 用户手动替换时做了全局替换，导致`PrivatePrivateDefinitions`，后用完整文件覆盖修复

### R2 — .NET Framework SDK缺失
**时间**: ~13:49 ~ 14:09
**错误**: `Could not find NetFxSDK install dir`，SwarmInterface模块无法实例化
**修复**: 通过 VS Installer 安装 .NET Framework 4.6.2 Targeting Pack + SDK
**注意**: 单独安装4.8 Developer Pack不够（系统说已有更高版本Runtime，但SDK/Targeting Pack没装对）
**状态**: 通过

### R3 — UHT 3个错误
**时间**: ~14:09
**错误**:
1. NeshamaConfig.h(39): 枚举UMETA ToolTip与UPROPERTY ToolTip重复
2. NPCSoulComponent.h(118): TMap默认参数 `Found '>' when expecting ','`
3. SocialEngine.h(113): TMap默认参数同上

**修复**:
1. 删除枚举的ToolTip元数据
2. TMap默认参数→拆重载函数（无默认参数版本）

**Commit**: 0530d9e
**打包**: v2

### R4 — FDialogueContext引擎名冲突
**时间**: ~14:20
**错误**: `Struct 'FDialogueContext' shares engine name 'DialogueContext' with struct 'FDialogueContext' in DialogueTypes.h`
**修复**: FDialogueContext → FSoulDialogueContext（4文件修改：NeshamaTypes.h, EntityMemory.h, NPCSoulComponent.h/cpp）
**Commit**: 78f1975
**打包**: v3

### R5 — FOnChatResponseDelegate名不匹配 + TArray默认参数
**时间**: ~14:25
**错误**:
1. NeshamaClient.h(208): `Unable to find 'class', 'delegate', 'enum', or 'struct' with name 'FOnChatResponseDelegate'`
2. InformationPropagator.h(150): `C++ Default parameter not parsed: Tags 'TArray()'`

**修复**:
1. NeshamaClient.h中引用的`FOnChatResponseDelegate`→`FOnChatResponse`（对齐NeshamaTypes.h中的声明）
2. InformationPropagator.h的TArray默认参数→拆重载函数

**Commit**: 679027e
**打包**: v4

### R6 — 6委托BlueprintAssignable冲突 + UFUNCTION重名 + EntityGraph预修
**时间**: ~14:32
**错误**:
1. NeshamaClient.h(266/272/278/284/290/296): `'BlueprintAssignable' is only allowed on multicast delegate properties`（6处）
2. InformationPropagator.h(154): `SpreadInformation conflicts`
3. SocialEngine.h(116): `RegisterNPC conflicts`
4. NPCSoulComponent.h(121): `SendGameEvent conflicts`

**修复**:
1. NeshamaTypes.h中6个事件委托从`DECLARE_DYNAMIC_DELEGATE`改为`DECLARE_DYNAMIC_MULTICAST_DELEGATE`
   - FOnNPCRegisteredEvent, FOnNPCUnregisteredEvent, FOnInformationSpreadEvent
   - FOnGameEventSentEvent, FOnRelationUpdatedEvent, FOnChatResponseEvent
2. UFUNCTION重载改名：
   - `SpreadInformation(full params)` → `SpreadInformationSimple(simple params)`
   - `RegisterNPC(full params)` → `RegisterNPCSimple(simple params)`
   - `SendGameEvent(full params)` → `SendGameEventSimple(simple params)`
3. EntityGraph.h的TArray默认参数预修（提前修复，避免下次再出错）

**Commit**: f9212dd
**打包**: v5

### R7 — 待验证
**状态**: v5已打包推送，用户需覆盖Plugins目录后重开UE5
**预判**:
- UHT阶段可能还有残余同名冲突或模板默认参数
- UHT通过后进入C++编译阶段，预计问题：
  - multicast委托不能BindLambda（需改AddLambda或分离单播回调委托）
  - NeshamaClient.h中6个BlueprintAssignable事件委托是否已全部改MULTICAST
  - UE 5.6 API兼容性

## 关键发现（写入TOOLS.md）

### UE5 UHT限制
1. `Definitions`不存在，必须用`PrivateDefinitions`/`PublicDefinitions`
2. UHT不支持模板类型默认参数（TArray<T>=TArray<T>()和TMap<K,V>=TMap<K,V>()）
3. UHT不支持同名UFUNCTION重载，必须改不同函数名
4. `BlueprintAssignable`只能用于MULTICAST委托
5. USTRUCT不能与引擎内置类型同名（如FDialogueContext）
6. 枚举UMETA的ToolTip不能与UPROPERTY的ToolTip重复

### 打包注意事项
- 必须cp到绝对路径 `/app/data/所有对话/主对话/` 用户才能下载
- 用`computer://`协议发送文件

## 提交历史
```
ec7b855 R1: Build.cs Definitions→PrivateDefinitions
0530d9e R3: 枚举ToolTip/TMap默认参数
78f1975 R4: FDialogueContext→FSoulDialogueContext
679027e R5: FOnChatResponseDelegate→FOnChatResponse + TArray
f9212dd R6: 6委托MULTICAST + UFUNCTION改名 + EntityGraph预修
```

## 待办
- [ ] R7验证（用户覆盖v5后重开UE5）
- [ ] C++编译阶段修复（BindLambda→AddLambda等）
- [ ] Dashboard JS改动推GitHub（14文件emoji/配色修复未提交）
